export { Alert, AlertDescription, AlertTitle, type AlertVariants } from '../__base/alert';

export {
  Alert as TbAlert,
  AlertDescription as TbAlertDescription,
  AlertTitle as TbAlertTitle,
} from '../__base/alert';
